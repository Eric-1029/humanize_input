<Window x:Class="HumanizeInput.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding UiWindowTitle}" Height="760" Width="980" MinHeight="520" MinWidth="680">
    <Viewbox Stretch="Uniform" StretchDirection="Both" Margin="10">
    <Grid Margin="18" Width="944" Height="724">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#F3F7FA" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding InputSectionTitle}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="220"
                         TextWrapping="Wrap"
                         FontSize="15"
                         Padding="10"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F6F3EF" CornerRadius="10" Padding="14" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="{Binding TypingRhythmSectionTitle}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding BaseDelayLabelText}" />
                    <Slider Minimum="20" Maximum="500" Value="{Binding BaseDelayMs}" TickFrequency="10" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding BaseDelayDisplayText}" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding JitterLabelText}" />
                    <Slider Minimum="0" Maximum="80" Value="{Binding JitterPercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding JitterDisplayText}" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding LeadInLabelText}" />
                    <Slider Minimum="0" Maximum="6000" Value="{Binding LeadInDelayMs}" TickFrequency="100" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding LeadInDisplayText}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#EEF7F0" CornerRadius="10" Padding="14" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding ErrorSimulationSectionTitle}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding TypoRateLabelText}" />
                    <Slider Minimum="0" Maximum="30" Value="{Binding TypoRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding TypoRateDisplayText}" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding OmissionRateLabelText}" />
                    <Slider Minimum="0" Maximum="30" Value="{Binding OmissionRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding OmissionRateDisplayText}" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding TransposeRateLabelText}" />
                    <Slider Minimum="0" Maximum="20" Value="{Binding TransposeRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding TransposeRateDisplayText}" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding RepairRateLabelText}" />
                    <Slider Minimum="0" Maximum="100" Value="{Binding RepairRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding RepairRateDisplayText}" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding ErrorDetectDelayLabelText}" />
                    <Slider Minimum="80" Maximum="3000" Value="{Binding ErrorDetectDelayMs}" TickFrequency="20" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding ErrorDetectDelayDisplayText}" Margin="0,0,0,8"/>

                    <TextBlock Text="{Binding BackspaceDelayLabelText}" />
                    <Slider Minimum="10" Maximum="500" Value="{Binding BackspaceDelayMs}" TickFrequency="5" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding BackspaceDelayDisplayText}" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#F2F2F2" CornerRadius="10" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="{Binding StartHotkeyLabelText}" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding StartHotkeyText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding HotkeyExampleTooltipText}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="{Binding PauseHotkeyLabelText}" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding PauseHotkeyText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding HotkeyExampleTooltipText}"/>
                </StackPanel>

                <Button Grid.Column="2" Content="{Binding ApplyHotkeysButtonText}" Width="110" Height="34" Margin="0,20,10,0" Command="{Binding ApplyHotkeysCommand}"/>

                <StackPanel Grid.Column="3" Margin="0,0,10,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding LanguageLabelText}" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                        <TextBlock Text="{Binding CurrentLanguageName}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <Button Content="{Binding LanguageToggleButtonText}" Width="70" Height="30" Command="{Binding ToggleLanguageCommand}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="4" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ProgressText}" FontSize="13" />
                    <TextBlock Text="{Binding TrayHintText}" FontSize="12" Foreground="#4A4A4A"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
    </Viewbox>
</Window>
