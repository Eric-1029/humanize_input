<Window x:Class="HumanizeInput.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding UiWindowTitle}"
        Height="730"
        Width="960"
        MinHeight="500"
        MinWidth="660"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Background="Transparent">
    <Viewbox Stretch="Uniform" StretchDirection="Both" Margin="4">
        <Border Background="#F8FBFE"
                BorderBrush="#D7E1EA"
                BorderThickness="1"
                CornerRadius="22"
                Padding="12"
                MouseLeftButtonDown="OnWindowFrameMouseLeftButtonDown">
            <Grid Margin="8" Width="944" Height="724">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="340"/>
                    <RowDefinition Height="132"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#F3F7FA" CornerRadius="10" Padding="12" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="{Binding InputSectionTitle}" FontSize="17" FontWeight="SemiBold" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 MinHeight="196"
                                 TextWrapping="Wrap"
                                 FontSize="14"
                                 Padding="9"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#F6F3EF" CornerRadius="10" Padding="12" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="{Binding TypingRhythmSectionTitle}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TextBlock Text="{Binding BaseDelayLabelText}"/>
                            <Slider Minimum="20" Maximum="500" Value="{Binding BaseDelayMs}" TickFrequency="10" IsSnapToTickEnabled="True"/>
                            <TextBlock Text="{Binding BaseDelayDisplayText}" Margin="0,0,0,6"/>

                            <TextBlock Text="{Binding JitterLabelText}"/>
                            <Slider Minimum="0" Maximum="80" Value="{Binding JitterPercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                            <TextBlock Text="{Binding JitterDisplayText}" Margin="0,0,0,6"/>

                            <TextBlock Text="{Binding LeadInLabelText}"/>
                            <Slider Minimum="0" Maximum="6000" Value="{Binding LeadInDelayMs}" TickFrequency="100" IsSnapToTickEnabled="True"/>
                            <TextBlock Text="{Binding LeadInDisplayText}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#EEF7F0" CornerRadius="10" Padding="12" Margin="6,0,0,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      CanContentScroll="False">
                            <StackPanel>
                                <TextBlock Text="{Binding ErrorSimulationSectionTitle}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding TypoRateLabelText}"/>
                                <Slider Minimum="0" Maximum="30" Value="{Binding TypoRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                                <TextBlock Text="{Binding TypoRateDisplayText}" Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding OmissionRateLabelText}"/>
                                <Slider Minimum="0" Maximum="30" Value="{Binding OmissionRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                                <TextBlock Text="{Binding OmissionRateDisplayText}" Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding TransposeRateLabelText}"/>
                                <Slider Minimum="0" Maximum="20" Value="{Binding TransposeRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                                <TextBlock Text="{Binding TransposeRateDisplayText}" Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding RepairRateLabelText}"/>
                                <Slider Minimum="0" Maximum="100" Value="{Binding RepairRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                                <TextBlock Text="{Binding RepairRateDisplayText}" Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding ErrorDetectDelayLabelText}"/>
                                <Slider Minimum="80" Maximum="3000" Value="{Binding ErrorDetectDelayMs}" TickFrequency="20" IsSnapToTickEnabled="True"/>
                                <TextBlock Text="{Binding ErrorDetectDelayDisplayText}" Margin="0,0,0,6"/>

                                <TextBlock Text="{Binding BackspaceDelayLabelText}"/>
                                <Slider Minimum="10" Maximum="500" Value="{Binding BackspaceDelayMs}" TickFrequency="5" IsSnapToTickEnabled="True"/>
                                <TextBlock Text="{Binding BackspaceDelayDisplayText}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#EDF2F6" CornerRadius="10" Padding="12,10" Margin="0,0,6,0">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding LanguageLabelText}" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding CurrentLanguageName}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <Button Content="{Binding LanguageToggleButtonText}" Width="62" Height="28" Command="{Binding ToggleLanguageCommand}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#F2F2F2" CornerRadius="10" Padding="10" Margin="6,0,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="230"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="{Binding StartHotkeyLabelText}" FontWeight="SemiBold"/>
                                <TextBox Text="{Binding StartHotkeyText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding HotkeyExampleTooltipText}"/>

                                <TextBlock Text="{Binding PauseHotkeyLabelText}" FontWeight="SemiBold" Margin="0,8,0,0"/>
                                <TextBox Text="{Binding PauseHotkeyText, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding HotkeyExampleTooltipText}"/>
                            </StackPanel>

                            <Button Grid.Column="1" Content="{Binding ApplyHotkeysButtonText}" Width="100" Height="32" Margin="0,26,8,0" Command="{Binding ApplyHotkeysCommand}"/>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding ProgressText}" FontSize="12"/>
                                <TextBlock Text="{Binding TrayHintText}" FontSize="11" Foreground="#4A4A4A"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Viewbox>
</Window>
