<Window x:Class="HumanizeInput.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="humanize_input" Height="760" Width="980" MinHeight="520" MinWidth="680">
    <Viewbox Stretch="Uniform" StretchDirection="Both" Margin="10">
    <Grid Margin="18" Width="944" Height="724">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#F3F7FA" CornerRadius="10" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="要输入的文本" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="220"
                         TextWrapping="Wrap"
                         FontSize="15"
                         Padding="10"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F6F3EF" CornerRadius="10" Padding="14" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="输入节奏" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="基础延迟 (ms)" />
                    <Slider Minimum="20" Maximum="500" Value="{Binding BaseDelayMs}" TickFrequency="10" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding BaseDelayMs, StringFormat=当前: {0} ms}" Margin="0,0,0,8"/>

                    <TextBlock Text="抖动百分比 (%)" />
                    <Slider Minimum="0" Maximum="80" Value="{Binding JitterPercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding JitterPercent, StringFormat=当前: {0}%}" Margin="0,0,0,8"/>

                    <TextBlock Text="开始前准备时间 (ms)" />
                    <Slider Minimum="0" Maximum="6000" Value="{Binding LeadInDelayMs}" TickFrequency="100" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding LeadInDelayMs, StringFormat=当前: {0} ms}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#EEF7F0" CornerRadius="10" Padding="14" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="错误模拟" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="错字率 (%)" />
                    <Slider Minimum="0" Maximum="30" Value="{Binding TypoRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding TypoRatePercent, StringFormat=当前: {0}%}" Margin="0,0,0,8"/>

                    <TextBlock Text="漏写率 (%)" />
                    <Slider Minimum="0" Maximum="30" Value="{Binding OmissionRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding OmissionRatePercent, StringFormat=当前: {0}%}" Margin="0,0,0,8"/>

                    <TextBlock Text="颠倒率 (%)" />
                    <Slider Minimum="0" Maximum="20" Value="{Binding TransposeRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding TransposeRatePercent, StringFormat=当前: {0}%}" Margin="0,0,0,8"/>

                    <TextBlock Text="修复率 (%)" />
                    <Slider Minimum="0" Maximum="100" Value="{Binding RepairRatePercent}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding RepairRatePercent, StringFormat=当前: {0}%}" Margin="0,0,0,8"/>

                    <TextBlock Text="发现错误后停顿 (ms)" />
                    <Slider Minimum="80" Maximum="3000" Value="{Binding ErrorDetectDelayMs}" TickFrequency="20" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding ErrorDetectDelayMs, StringFormat=当前: {0} ms}" Margin="0,0,0,8"/>

                    <TextBlock Text="回删间隔 (ms)" />
                    <Slider Minimum="10" Maximum="500" Value="{Binding BackspaceDelayMs}" TickFrequency="5" IsSnapToTickEnabled="True"/>
                    <TextBlock Text="{Binding BackspaceDelayMs, StringFormat=当前: {0} ms}" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#F2F2F2" CornerRadius="10" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="开始热键" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding StartHotkeyText, UpdateSourceTrigger=PropertyChanged}" ToolTip="示例: Ctrl+Alt+S"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="暂停/继续热键" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding PauseHotkeyText, UpdateSourceTrigger=PropertyChanged}" ToolTip="示例: Ctrl+Alt+P"/>
                </StackPanel>

                <Button Grid.Column="2" Content="应用热键" Width="100" Height="34" Margin="0,20,10,0" Command="{Binding ApplyHotkeysCommand}"/>

                <StackPanel Grid.Column="3" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ProgressText}" FontSize="13" />
                    <TextBlock Text="程序启动后会自动最小化到托盘。双击托盘图标可恢复窗口。" FontSize="12" Foreground="#4A4A4A"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
    </Viewbox>
</Window>
